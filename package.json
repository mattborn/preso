{
  "name": "react-sketchapp-starter",
  "version": "1.0.0",
  "description": "",
  "skpm": {
    "main": "react-sketchapp-starter.sketchplugin",
    "manifest": "src/manifest.json"
  },
  "scripts": {
    "build": "skpm-build",
    "watch": "skpm-build --watch",
    "render": "skpm-build --watch --run",
    "render:once": "skpm-build --run",
    "postinstall": "npm run build && skpm-link"
  },
  "author": "Matt Born <code@mattborn.com>",
  "license": "MIT",
  "devDependencies": {
    "@skpm/builder": "^0.5.11"
  },
  "dependencies": {
    "prop-types": "^15.5.8",
    "react": "^16.3.2",
    "react-sketchapp": "^2.1.0",
    "react-test-renderer": "^16.3.2"
  }
}
